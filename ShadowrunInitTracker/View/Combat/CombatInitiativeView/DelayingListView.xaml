<UserControl x:Class="ShadowrunInitTracker.View.DelayingListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ShadowrunInitTracker.View"
             xmlns:core="clr-namespace:System;assembly=mscorlib"
             xmlns:model="clr-namespace:ShadowrunInitTracker.Model">
    <UserControl.CommandBindings>
        <CommandBinding Command="{x:Static local:DelayingListView.EndDelayActorCmd}" Executed="EndDelayActor"/>
    </UserControl.CommandBindings>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding Title, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type local:DelayingListView}}}"/>
        <ListView Grid.Row="1" ItemsSource="{Binding ItemsSource, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type local:DelayingListView}}}" SelectedIndex="{Binding CurrentEntryIndex, Mode=OneWay}" SelectionMode="Single" Focusable="False">
            <ListView.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Act" Command="{x:Static local:DelayingListView.EndDelayActorCmd}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=ContextMenu}, Path=PlacementTarget.SelectedItem}"/>
                </ContextMenu>
            </ListView.ContextMenu>
            <ListView.View>
                <GridView AllowsColumnReorder="False">
                    <GridView.Columns>
                        <GridViewColumn Header="Name" Width="150" DisplayMemberBinding="{Binding Description}"/>
                        <GridViewColumn Header="Phase" Width="150" DisplayMemberBinding="{Binding PhaseDescription}"/>
                    </GridView.Columns>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>

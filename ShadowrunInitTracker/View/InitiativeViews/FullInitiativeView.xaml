<UserControl x:Class="ShadowrunInitTracker.View.FullView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ShadowrunInitTracker.View"
             xmlns:vm="clr-namespace:ShadowrunInitTracker.ViewModel"
             mc:Ignorable="d" >
    <UserControl.DataContext>
        <vm:FullInitiativeViewModel/>
    </UserControl.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>
        <local:PhaseGrid Title="Current Initiative" DataContext="{Binding CurrentInit}"/>
        <local:PhaseGrid Title="Original Initiative" Grid.Row="1" DataContext="{Binding OriginalInit}"/>
        <Button Content="Next" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Left"/>
    </Grid>
</UserControl>
